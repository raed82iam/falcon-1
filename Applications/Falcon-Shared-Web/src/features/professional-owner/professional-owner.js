import { safeText } from '../../security/safe-html.js';

export function createProfessionalOwnerFeature({language,workspace,data}={}){
  const ar=()=>language()==='ar';
  const tx=(a,e)=>safeText(ar()?a:e);
  const apps=[
    ['Foundation','CORE','FOUNDATION','READY',tx('التشغيل الفعلي غير مفعّل','Runtime activation is not enabled')],
    ['FSATS Trading','TRADING','FSATS','BLOCKED',tx('اتصال الوسيط مغلق','Broker connectivity is disabled')],
    ['FSAPMA','DATA','FSATS','BLOCKED',tx('مسارات المزودين غير مربوطة','Provider routes are not bound')],
    ['Trading Guardian','GUARD','FSATS','READY',tx('مسار أوامر الحماية بانتظار الربط','Protection command route awaits binding')],
    ['FSTSimA','SIM','FSATS','READY',tx('محاكاة غير حية فقط','Non-live simulation only')],
    ['APP-RSC','RESOURCE','FSATS','PENDING',tx('ربط موارد Foundation قيد الانتظار','Foundation resource binding is pending')]
  ];
  const appCards=apps.map(([name,type,owner,state,issue])=>`<article class="ops-app"><div class="ops-app-icon">${safeText(type.slice(0,2))}</div><div><b>${safeText(name)}</b><small>${safeText(owner)} • ${safeText(type)}</small><p>${issue}</p></div><span class="ops-state ${state.toLowerCase()}">${safeText(state)}</span></article>`).join('');
  const issues=[
    ['CRITICAL',tx('Runtime registration غير منفذ','Runtime registration has not been executed'),'Foundation / FSATS'],
    ['HIGH',tx('Broker execution egress مقفل','Broker execution egress is disabled'),'Trading'],
    ['HIGH',tx('مزودو بيانات السوق غير متصلين','Market-data providers are disconnected'),'FSAPMA'],
    ['MEDIUM',tx('هوية الإنتاج وMFA غير مربوطين','Production identity and MFA are not bound'),'Shared Web'],
    ['MEDIUM',tx('ربط موارد APP-RSC غير مكتمل','APP-RSC resource binding is incomplete'),'APP-RSC']
  ];
  const issueRows=issues.map(([level,text,source],i)=>`<div class="ops-issue"><span class="${level.toLowerCase()}">${level}</span><div><b>${text}</b><small>${safeText(source)}</small></div><time>${['10:42','10:18','09:54','09:31','08:47'][i]}</time></div>`).join('');
  const services=Array.isArray(data.services)?data.services:[];
  const nodes=apps.map(([name,,owner,state])=>`<div class="topology-node ${state.toLowerCase()}"><i></i><b>${safeText(name)}</b><small>${safeText(owner)} • ${safeText(state)}</small></div>`).join('');
  function owner(){
    return workspace(`<div class="fin-page ops-page"><header class="fin-page-head"><div><span class="fin-eyebrow">FALCON OS • OWNER</span><h1>${tx('مركز قيادة النظام','System command center')}</h1><p>${tx('مراقبة التطبيقات والتبعيات والمشاكل من مكان واحد. لا توجد صلاحية تداول ضمن هذه الشاشة.','Monitor applications, dependencies and problems from one place. This surface grants no trading authority.')}</p></div><div class="ops-clock"><strong>10:42:18</strong><small>${tx('بيئة تجريبية محلية','Local safe demo')}</small></div></header><section class="ops-kpis"><article><small>${tx('الصحة العامة','Overall health')}</small><strong class="gain">98%</strong><span>${tx('5 خدمات مستقرة','5 services stable')}</span></article><article><small>${tx('التطبيقات المسجلة','Registered apps')}</small><strong>0 / 6</strong><span>${tx('التسجيل الفعلي غير منفذ','Actual registration not executed')}</span></article><article><small>${tx('مشاكل حرجة','Critical issues')}</small><strong class="loss">1</strong><span>${tx('تحتاج قرار المالك','Owner decision required')}</span></article><article><small>${tx('روابط خارجية','External routes')}</small><strong>0</strong><span>${tx('كلها مقفلة بأمان','All safely disabled')}</span></article><article><small>${tx('طلبات معلقة','Pending actions')}</small><strong>5</strong><span>${tx('لا تنفيذ تلقائي','No automatic execution')}</span></article></section><div class="ops-grid"><section class="fin-panel topology-panel"><div class="fin-panel-head"><div><h3>${tx('خريطة النظام والتطبيقات','System and application topology')}</h3><small>${tx('حالة الربط الحالية','Current binding state')}</small></div><span class="ops-state preview">PREVIEW</span></div><div class="topology"><div class="kernel"><span>F</span><b>Falcon Foundation</b><small>${tx('النواة التقنية','Technical core')}</small></div><div class="topology-lines"></div><div class="topology-nodes">${nodes}</div></div></section><section class="fin-panel resource-panel"><div class="fin-panel-head"><h3>${tx('الموارد التقنية','Technical resources')}</h3><span class="gain">${tx('ضمن الحدود','Within limits')}</span></div><div class="resource-bars"><div><header><b>CPU</b><span>23%</span></header><i><em style="width:23%"></em></i><small>64 Cores</small></div><div><header><b>Memory</b><span>41%</span></header><i><em style="width:41%"></em></i><small>256 GB</small></div><div><header><b>Storage</b><span>34%</span></header><i><em style="width:34%"></em></i><small>8 TB</small></div><div><header><b>Network</b><span>18%</span></header><i><em style="width:18%"></em></i><small>${tx('اتصال خارجي مقفل','External egress disabled')}</small></div></div></section><section class="fin-panel issues-panel"><div class="fin-panel-head"><div><h3>${tx('المشاكل التي تحتاج متابعة','Issues requiring attention')}</h3><small>${tx('مرتبة حسب الخطورة','Ordered by severity')}</small></div><button data-nav="owner-incidents">${tx('كل الحوادث','All incidents')}</button></div>${issueRows}</section><section class="fin-panel applications-panel"><div class="fin-panel-head"><div><h3>${tx('حالة التطبيقات','Application status')}</h3><small>${tx('الجاهزية والربط والمشكلة الحالية','Readiness, binding and current issue')}</small></div><button data-nav="owner-apps">${tx('التفاصيل','Details')}</button></div><div class="ops-apps">${appCards}</div></section><aside class="fin-panel owner-copilot"><div class="copilot-head"><div class="copilot-mark">F</div><div><h3>Falcon AI</h3><small>${tx('مساعد المالك','Owner copilot')}</small></div><span class="ops-state blocked">OFFLINE</span></div><div class="copilot-message"><b>${tx('ملخص الحالة','Status summary')}</b><p>${tx('البنية البرمجية جاهزة للتحضير، لكن التشغيل الفعلي والوسطاء والمزودين والهوية الإنتاجية ما زالت غير مربوطة. أوصي بإكمال Runtime Registration أولًا، ثم الهوية، ثم بيانات السوق، وأخيرًا Paper Trading.','The software baseline is preparation-ready, but runtime, brokers, providers and production identity remain unbound. Complete runtime registration first, then identity, market data, and finally paper trading.')}</p></div><div class="copilot-actions"><button>${tx('لخص المشاكل الحرجة','Summarize critical issues')}</button><button>${tx('اعرض خطة الربط','Show integration plan')}</button><button>${tx('راجع حالة التطبيقات','Review application status')}</button></div><div class="copilot-input"><input disabled placeholder="${tx('المحادثة غير مربوطة بعد','Chat is not bound yet')}"><button disabled>➤</button></div></aside></div></div>`,'owner',true);
  }
  return Object.freeze({owner});
}
